# Import bibliotek
import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.tree import DecisionTreeClassifier
from sklearn.metrics import accuracy_score, precision_score, recall_score, f1_score

# Dane
data = {
    'TP53_expr': [2.1, 8.5, 1.8, 6.2, 7.9, 3.1, 9.2, 2.8],
    'BRCA1_expr': [3.4, 7.2, 2.5, 6.1, 6.8, 4.0, 7.9, 3.9],
    'TF_motifs': [2, 6, 1, 4, 5, 2, 6, 3],
    'KRAS': [1.2, 7.1, 0.9, 6.8, 1.5, 5.5, 1.0, 6.3],
    'Cancer_status': [0, 1, 0, 1, 1, 0, 1, 0]
}

df = pd.DataFrame(data)

# Podział na cechy i etykiety
X = df.drop('Cancer_status', axis=1)
y = df['Cancer_status']

# Podział na zbiór treningowy i testowy
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.25, random_state=42)

# Model
model = DecisionTreeClassifier(random_state=42)

# Trenowanie
model.fit(X_train, y_train)

# Predykcja
y_pred = model.predict(X_test)

# Metryki
accuracy = accuracy_score(y_test, y_pred)
precision = precision_score(y_test, y_pred)
recall = recall_score(y_test, y_pred)
f1 = f1_score(y_test, y_pred)

# Wyniki
print(f"Accuracy: {accuracy:.3f}")
print(f"Precision: {precision:.3f}")
print(f"Recall: {recall:.3f}")
print(f"F1-score: {f1:.3f}")

# Komentarz:
#Accuracy: 1.000
#Precision: 1.000
#Recall: 1.000
#F1-score: 1.000
# Ze względu na bardzo mały zbiór danych (tylko 8 próbek), wyniki metryk mogą być niestabilne i silnie zależne od podziału na zbiór treningowy i testowy.
# Model drzewa decyzyjnego może łatwo dopasować się idealnie do danych treningowych (overfitting).
# Jeśli accuracy i pozostałe metryki są wysokie (np. 1.0), nie oznacza to dobrej generalizacji, lecz raczej dopasowanie do małej próbki.
# Aby uzyskać bardziej wiarygodną ocenę, należałoby użyć większego zbioru danych lub walidacji krzyżowej (cross-validation).
